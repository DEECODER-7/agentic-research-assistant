# LLM + orchestration
langchain>=0.2.0
langchain-community>=0.2.0
langchain-groq>=0.1.5
langchain-text-splitters>=0.2.0
langgraph>=0.1.0

# Embeddings — called remotely via HF's Inference API rather than loaded
# locally, to keep the running process light enough for Render's free tier
langchain-huggingface>=0.1.0
huggingface-hub>=0.24.0

# Vector store
chromadb>=0.5.0
langchain-chroma>=0.1.4

# Document sourcing
arxiv>=2.1.0
pypdf>=4.2.0

# Web search fallback (free, no API key)
tavily-python>=0.5.0

# API + UI
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
streamlit>=1.32.0
requests>=2.31.0

# Utilities
python-dotenv>=1.0.0
pytest>=8.0.0